print("hello world")
vec[1]
vec <- c(1,2,3,4,5)
vec[1]
vec[length(vec)-1]
vec[length(vec)]
#First two components
vec[1:2]
#Last two components
vec[length(vec):-1]
#Last two components
vec[length(vec):1]
#Last two components
vec[length(vec):length(vec)-1]
#Last two components
vec[length(vec):length(vec)-2]
#Last two components
vec[length(vec)-1:length(vec)]
#Last two components
vec[length(vec)]
#Last two components
vec[length(vec),length(vec)-1]
#Last two components
vec[length(vec),"length(vec)-1"]
#Last two components
vec[length(vec)-1]
#Last two components
vec[5:1]
#Last two components
vec[5:4]
#Last two components using the c notiation
vec[c(length(vec),length(vec)-1)]
#Are two vectors equal?
v1<-[8,9,10]
#Are two vectors equal?
v1<-c(8,9,10)
v2<-c(2*4,3*3,5*2)
v1==v2
#Give the vectors some names
names(vec)<- c("one","two","three","four","five")
vec
vec["three"]
vec$two
#Reference a specific vector
vec["three"]
#Reference a specific vector
vec["three"]
vec$two
vec$`two`
#Return only the values of a vector
unnnamed(vec)
#Return only the values of a vector
unnamed(vec)
#Return only the values of a vector
unname(vec)
#Append to vector
vec<-c(vec,6,7,8,9)
#Ceck the length of a vector
length(vec )
#Ceck the length of a vector
length(vec)
dim(vec)
max(vec)
min(vec)
mean(vec)
mode(vec)
typeof(vec)
class(vec)
#get to know your vec - 10 steps
str(vec)
summary(vec)
table(vec)
View(vec)
max(vec)
min(vec)
mean(vec)
median(vec)
typeof(vec)
class(vec)
length(vec)
plot(vec)
hist(vec)
colnames(vec)
head(vec)
tail(vec)
#Remove from vector
vec<-vec[-2]
vec
#Injects component at a particular location
vec[2]<-2
vec
#remove comp
#combine vectors
v1+v2
#conditional selection of a vector
cvec <- vec>5
cvec
#conditional selection of a vector
cvec <- vec[2:4]>5
cvec
#conditional selection of a vector
cvec <- vec>5
cvec
sum(vec)
#conditional selection of a vector in two steps
cvecindex <- vec>5
cvec <-vec[cvecindex]
cvec
#conditional selection of a vector in two steps
cvecindex <- vec>5
cvec <-vec[cvecindex]
cvec
